# --- Core ---
python-dotenv==1.0.1
pydantic==2.9.2
pydantic-settings==2.6.1

# --- PDF parsing ---
pdfplumber==0.11.4

# --- Retrieval: BM25 (local, lightweight, no ML) ---
rank-bm25==0.2.2

# --- Cloud services: STT/LLM/TTS (Groq), Hinglish TTS (Sarvam),
# embeddings + reranking (Cohere), vector database (Chroma Cloud) ---
groq==0.13.0
cohere>=5.6.0
chromadb>=0.5.20
httpx==0.27.2
numpy==2.1.3

# --- Server ---
fastapi==0.115.5
uvicorn[standard]==0.32.1
python-multipart==0.0.19
websockets==14.1